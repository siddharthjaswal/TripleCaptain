/** @type {import("prettier").Config} */
const config = {
  arrowParens: "always",
  singleQuote: false,
  trailingComma: "all",
  tabWidth: 2,
  semi: true,
  plugins: [],
};

export default config;
